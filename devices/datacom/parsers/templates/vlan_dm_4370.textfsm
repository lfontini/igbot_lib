Value VLAN_ID (\d+)
Value VLAN_NAME (\S+)
Value VLAN_TYPE (\S+)
Value VLAN_COUNT (\d+)
Value IFACE (\S+)
Value STATUS (\S+)
Value PORT_STATE (\S+)

Start
  ^-+ -> Start
  # Linha principal da VLAN
  ^\s*${VLAN_ID}\s+${VLAN_NAME}\s+${VLAN_TYPE}\s+${VLAN_COUNT}\s+${IFACE}\s+${STATUS}\s+${PORT_STATE} -> Record
  # Linha de interface adicional
  ^\s+${IFACE}\s+${STATUS}\s+${PORT_STATE} -> Record
